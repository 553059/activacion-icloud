customtkinter
pillow
packaging
pytest

# Captive portal / profile generation / docs
Flask>=2.0
flask-cors
qrcode
Pillow
reportlab  # optional (only required for PDF export from recovery_docs)

# HTTPS and profile signing
cryptography
requests

# DNS interception and websocket notifications
dnslib
flask-sock
